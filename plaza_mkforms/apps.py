"""Django app configuration for plaza_mkforms.

This module contains the configuration class for the plaza_mkforms Django app, which is
used to initialize and configure the app within the Django project.
"""

import django.apps


class PlazaMkformsConfig(django.apps.AppConfig):
    """Configuration for the plaza_mkforms Django app.

    This class defines the app's name and any other configuration settings required for
    the plaza_mkforms app to function properly within the Django framework.
    """

    name = "plaza_mkforms"
